<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Flutter hanya menyisipkan INTERNET ke manifest debug & profile.
         Tanpa baris ini, build RELEASE tidak bisa menghubungi Supabase sama
         sekali — dan gejalanya hanya muncul setelah aplikasi dipaketkan,
         bukan saat pengembangan. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- ============================================================
         Izin perekaman (Bab 8.9).

         Bab 8.9 menegaskan: minta izin SAAT PERTAMA KALI DIBUTUHKAN, bukan
         semuanya di awal aplikasi. Deklarasi di sini hanya membuat izinnya
         mungkin diminta; permintaannya sendiri diatur PermissionService.
         ============================================================ -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- Lokasi bersifat OPSIONAL. Bila ditolak, perekaman tetap berjalan dan
         watermark berbunyi "Lokasi tidak tersedia" (Bab 8.9). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Notifikasi progres upload. Wajib diminta di Android 13+; bila ditolak,
         upload tetap jalan tanpa notifikasi (Bab 8.9). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Antrian upload harus bertahan saat aplikasi di latar (Bab 8.7). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Unduhan video. READ_EXTERNAL_STORAGE hanya relevan sampai Android 12;
         sejak Android 13 penyimpanan memakai izin bermedia terpisah. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>

    <!-- Kamera dinyatakan TIDAK wajib agar aplikasi tetap dapat dipasang di
         perangkat tanpa kamera — Owner dan Admin memakainya hanya untuk
         memantau, tidak pernah merekam (Bab 2.2). -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>

    <application
        android:label="KamelScan"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Deep link verifikasi email, reset password, dan callback OAuth
                 (Bab 6.4 & 6.5).

                 Skema harus SAMA PERSIS dengan AUTH_REDIRECT_SCHEME di
                 env.dev.json, dan URL lengkapnya WAJIB didaftarkan di
                 Supabase Dashboard > Authentication > URL Configuration >
                 Redirect URLs. Bila tidak terdaftar, Supabase mengabaikannya
                 dan memakai Site URL — pengguna mendarat di halaman web,
                 bukan kembali ke aplikasi. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="id.kamelscan.app"/>
            </intent-filter>
        </activity>
        <!-- Layar pemotong foto milik image_cropper (Bab 9.6).

             🔴 WAJIB dideklarasikan di sini. Tanpa baris ini aplikasi
             **mati total** begitu Owner memilih foto profil: Android menolak
             membuka layar yang tidak terdaftar
             (ActivityNotFoundException: com.yalantis.ucrop.UCropActivity),
             lalu plugin-nya membalas Flutter dua kali dan proses dimatikan
             (IllegalStateException: Reply already submitted).

             Terbukti di Redmi Note 9, 20 Agustus 2026. Tidak terdeteksi oleh
             `flutter analyze` maupun `flutter test` karena ini berkas
             konfigurasi Android, bukan kode Dart — satu-satunya cara
             menemukannya adalah menjalankannya di perangkat sungguhan.

             Temanya memakai AppCompat karena UCrop mewarisi
             AppCompatActivity; tema aplikasi biasa akan membuatnya menolak
             mulai. -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
